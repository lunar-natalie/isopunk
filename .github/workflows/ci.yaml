name: CI
on:
  push:
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        run: sudo apt-get install -y libsdl2-dev libboost-all-dev libvulkan-dev libglm-dev cppcheck

      - name: Check out repository
        uses: actions/checkout@v3.3.0

      - name: Configure and build
        uses: threeal/cmake-action@latest
        with:
          generator: Ninja
          cxx-compiler: clang++
          run-test: true
